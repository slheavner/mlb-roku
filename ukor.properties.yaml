name: ukor-sample-project
version: 0.0.1
buildDir: build
sourceDir: src
defaults:
  flavor: main
rokus: 
  premiere:
    serial: 'SERIAL123456'
    auth: 
      user: 'rokudev'
      pass: 'yourpassword'
flavors:
  main:
    src:
      - main
  # adds the tracker task for the advanced layout editor
  debug:
    src: 
      - main
      - debug
  test:
    src: 
      - main
      - test
